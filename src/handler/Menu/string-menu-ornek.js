

module.exports = {
    value: 'add_user_to_ticket', // Örnek Value
    type: 'USER_SELECT_MENU', // Bu önemli!
    
    async execute(interaction) {
        if (!interaction.isUserSelectMenu()) return;

       
}

}